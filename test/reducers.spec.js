import { expect }       from 'chai';
import Reducers         from 'reducers';
import ReducersInjector from 'inject-loader!reducers';

describe('Reducers :: ', () => {
	it('should pass', () => {
		expect(true).to.be.true;
	});
});
